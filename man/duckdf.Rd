\name{duckdf}
\alias{duckdf}
\title{duckdf}
\usage{
duckdf("SELECT mpg, cyl FROM mtcars WHERE disp >= 200")
}
\description{
Like SQL for dataframes, but with ducks.
In fact, it's ducks all the way down
}
\examples{
duckdf()
}
